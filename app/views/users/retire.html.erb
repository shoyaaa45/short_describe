<div class = "page-header">
  <h1>退会の確認</h1>
</div>
<% if current_user.errors.any? %>
  <div class = "alert alert-danger">
    <ul>
      <% current_user.errors.full_message.each do |msg| %>
        <li><%= msg %>
      <% end %>
    </ul>
  </div>
<% end %>
<p>退会した場合、ユーザーデータが削除されます。</p>
<p>本当に退会しますか?</p>
<%= link_to '退会', user_path, method: :delete, class: 'btn btn-danger' %>
